// react hot toast 
  //  -> library 
  //  -> easy to show notification

// useContext and context provider 
  // -> create context when we have share same in multiple props
    // -> create a context object that passes the data to other component without props
    // -> like creating a radio station
  // -> theme context 
    // -> name for the context object 
    // -> has two properties provider and consumer to pass data without prop
  // -> themeprovider manage and provide shared data to other component 
  // -> children prop placeholder for the children
  // -> we access data using usestate

// const navigate = useNavigate();
  // -> use for the navigation

// try-catch block 
  // -> not something new but used to handle errors in the react app

// async and await 

